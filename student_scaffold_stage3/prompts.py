from __future__ import annotations


EXTRA_RULES = [
    "When learned memory gives a preferred time window, use it only to choose among valid free slots.",
    "Do not invent a preference if the learned extractor did not store one.",
]
